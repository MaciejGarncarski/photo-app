{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "dotEnv": [".env"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "format": {},
    "start": {},
    "check-types": {},
    "dev": {
      "cache": false
    }
  }
}
